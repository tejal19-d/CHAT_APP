{
  "name": "chatty-monorepo",
  "version": "1.0.0",
  "description": "Monorepo scripts to build and deploy fullstack Chatty",
  "scripts": {
    "install-backend": "cd backend && npm install",
    "install-frontend": "cd frontend/chat_app && npm install",
    "build-frontend": "cd frontend/chat_app && npm run build",
    "build": "npm run install-backend && npm run install-frontend && npm run build-frontend",
    "start": "cd backend && node src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
